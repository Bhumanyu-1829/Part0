sequenceDiagram
    actor User
    participant Browser
    participant Server

    Note over User, Browser: 1. User enters text and clicks "Save"
    User->>Browser: Clicks Save button

    Note right of Browser: 2. JavaScript prevents default form submit,<br/>creates new note object, adds to local array,<br/>and re-renders DOM to show the new note instantly.
    
    Browser->>Server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate Server
    Note right of Server: 3. Server receives JSON data,<br/>creates note object, and saves it.
    Server-->>Browser: 201 Created (or status 200 OK with confirmation)
    deactivate Server

    Note right of Browser: 4. Server responds with JSON confirmation.<br/>No page redirect or full reload occurs.